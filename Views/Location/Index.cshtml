@model List<HealthNCare.Models.Location>

<h2>Locations</h2>

<ul>
    @foreach (var location in Model)
    {
        <li>
            <a asp-controller="Hospital" asp-action="Details" asp-route-id="@location.LocationId">@location.Name</a>
        </li>
    }
</ul>
